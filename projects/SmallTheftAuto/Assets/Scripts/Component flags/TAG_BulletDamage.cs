using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// TODO: You know that I love using components as Flags, but I don't think that you need to name them TAG_XX
// Maybe just `BulletDamageable` or `BulletDamage`?
public class TAG_BulletDamage : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
